package app

const (
	// ServerCertPath is the path to the server certificate
	ServerCertPath = "certificates/server.crt"

	// ServerKeyPath is the path to the server key
	ServerKeyPath = "certificates/server.pem"
)
